<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.coffeereview.mapper.UserMapper">

<insert id="insertUser" >
	insert into tbl_users (uid, name, nickname, age, gender, phonenum, email, address)
	values (#{uid}, #{name}, #{nickname}, #{age}, #{gender}, #{phonenum}, #{email}, #{address})
</insert>

<select id="readUser" resultType="com.coffeereview.domain.UserVO">
	select * from tbl_users where uid = #{uid}
</select>

<delete id="deleteUser" >
	delete from tbl_users where uid = #{uid}
</delete>

<update id="updateUser">
	update tbl_users
	name = #{name},
	nickname = #{nickname},
	age = #{age};
	gender = #{gender},
	phonenum = #{phonenum},
	email = #{email},
	address = #{address}
	where uid = #{uid}

</update>

<select id="findUser" resultType="boolean">
	select if(count(*) != 0, 1, 0) <!-- uid를 갖는 tuple이 존재하면 1, 아니면 0 반환 -->
	from tbl_users
	where uid = #{uid}
</select>










</mapper>